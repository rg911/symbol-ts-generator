export enum BuildInType {
    Byte = 'byte',
    Struct = 'struct',
    Enum = 'enum',
}

export enum DispositionType {
    Inline = 'inline',
    Const = 'const',
    Array = 'array',
    ArrayFill = 'array fill',
    ArraySized = 'array sized',
    Reserved = 'reserved',
}
