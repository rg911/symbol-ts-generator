export enum BuildInType {
    Byte = 'byte',
    Struct = 'struct',
    Enum = 'enum',
}

export enum DispositionType {
    Inline = 'inline',
    Const = 'const',
    Array = 'array',
    FillArray = 'array fill',
    reserved = 'reserved',
}
